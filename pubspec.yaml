name: transport_app
description: A new Flutter project.

# The following line prevents the package from being accidentally published to
# pub.dev using `pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  auto_size_text: 3.0.0
  cached_network_image: ^3.4.1
  octo_image: ^2.1.0
  flutter_blurhash: ^0.9.1
  collection: ^1.18.0
  dropdown_button2: 2.3.9
  easy_debounce: ^2.0.3
  equatable: ^2.0.7
  flutter_animate: ^4.5.2
  flutter_cache_manager: ^3.4.1
  flutter_datetime_picker_plus: ^2.2.0
  flutter_plugin_android_lifecycle: ^2.0.33
  font_awesome_flutter: ^11.0.0
  from_css_color: 2.0.0
  go_router: ^17.0.0
  internet_file: ^1.3.0
  pdfx: ^2.9.2
  google_fonts: ^6.3.3
  http: 1.2.2
  image_picker: ^1.2.1
  image_picker_android: ^0.8.13+10
  image_picker_for_web: ^3.1.1
  image_picker_ios: ^0.8.13+3
  image_picker_platform_interface: ^2.11.1
  infinite_scroll_pagination: ^4.0.0
  intl: ^0.20.2
  signature: ^6.3.0
  json_path: ^0.8.0
  mime_type: ^1.0.1
  page_transition: ^2.2.1
  path_provider: ^2.1.5
  path_provider_android: ^2.2.22
  path_provider_foundation: ^2.5.1
  path_provider_platform_interface: ^2.1.2
  percent_indicator: ^4.2.5
  plugin_platform_interface: 2.1.8
  provider: ^6.1.2
  shared_preferences: ^2.5.4
  shared_preferences_android: ^2.4.18
  shared_preferences_foundation: ^2.5.6
  shared_preferences_platform_interface: ^2.4.1
  shared_preferences_web: ^2.4.3
  sqflite: ^2.4.2
  timeago: ^3.7.1
  url_launcher_android: ^6.3.28
  url_launcher_platform_interface: 2.3.2
  uuid: ^4.0.0
  video_player: ^2.10.1
  video_player_android: ^2.8.22
  video_player_avfoundation: ^2.8.8
  video_player_platform_interface: ^6.6.0
  video_player_web: ^2.4.0
  webview_flutter: ^4.9.0
  webview_flutter_android: ^3.16.9
  webview_flutter_platform_interface: ^2.14.0
  webview_flutter_wkwebview: ^3.23.5
  webviewx_plus: ^0.5.1
  html: ^0.15.0
  photo_view: ^0.15.0
  flutter_quill: 11.5.1
  flutter_quill_extensions: ^11.0.0
  file_picker: ^10.3.7
  url_launcher: ^6.1.7
  url_launcher_ios: ^6.2.4
  flutter_custom_tabs: ^2.4.0
  math_expressions: ^2.6.0
  google_maps: ^8.0.0
  google_maps_flutter: ^2.17.0
  google_maps_flutter_android: ^2.19.8
  google_maps_flutter_ios: ^2.18.1
  google_maps_flutter_platform_interface: ^2.15.0
  google_maps_flutter_web: ^0.6.2
  map_launcher: 3.5.0
  geolocator: ^13.0.2
  image: ^4.5.4
  web_socket_channel: ^3.0.1


  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.0
  firebase_messaging: ^16.0.4
  firebase_core: ^4.2.1
  firebase_analytics: ^12.0.4
  flutter_local_notifications: ^19.5.0
  local_auth: ^2.3.0
  flutter_secure_storage: ^9.2.4
  app_links: ^6.3.2
  record: ^6.2.1
  audioplayers: ^6.7.1
  http_parser: ^4.1.2
  flutter_markdown_plus: ^1.0.12
  cross_file: ^0.3.5+4
  sembast: ^3.8.6
  sembast_web: ^2.4.4+1

dependency_overrides:
  http: 1.2.2
  uuid: ^4.0.0

dev_dependencies:
  flutter_launcher_icons: ^0.14.4
  flutter_lints: ^6.0.0
  lints: ^6.0.0

  flutter_test:
    sdk: flutter


flutter_launcher_icons:
  android: true
  ios: true
  remove_alpha_ios: true
  adaptive_icon_background: 'assets/images/app_launcher_background_v2.png'
  adaptive_icon_foreground: 'assets/images/app_launcher_foreground_v4.png'
  adaptive_icon_foreground_inset: 22
  adaptive_icon_monochrome: 'assets/images/app_launcher_monochrome_v4.png'
  image_path_ios: 'assets/images/app_launcher_icon_light_v4.png'
  image_path_ios_dark_transparent: 'assets/images/app_launcher_icon_dark_transparent_v4.png'
  web:
    generate: true
  image_path: 'assets/images/app_launcher_icon_light_v4.png'


# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/fonts/
    - assets/images/
    - assets/videos/
    - assets/audios/
    - assets/lottie_animations/
    - assets/rive_animations/
    - assets/pdfs/


  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware.

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages
